using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class chickenScript : MonoBehaviour
{

    public GameObject tavuk;
    public GameObject speaker1;
    public GameObject speaker2;

    void Start()
    {
        
    }

    
    void Update()
    {
        /*if (Input.GetMouseButton(0))
        {

            if (this.tavuk.transform.position.y < 6.1f && this.tavuk.transform.position.y > -6.2f && this.tavuk.transform.position.x < 3.0f && this.tavuk.transform.position.x > -3.0f)
            {
                tavuk.transform.position += speaker1.transform.forward * Time.deltaTime * 5;

            }
            else
            {
                Application.LoadLevel("Failed");
            }
        }*/
    }

   /* private void OnMouseDown()
    {
        tavuk.transform.position += hoparlör.transform.forward * vector3.distance(tavuk, hoparlör);
    }*/
}

/*

tavuk.transform.position += hoparlör.transform.forward*vector3.distance(tavuk,hoparlör)


 */